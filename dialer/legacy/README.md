# Retired

`sheet-sync.gs` is the Google Sheets backend from before accounts existed. It is kept here so an existing sheet keeps working if you ever point the old standalone app at it.

The current app stores everything in Supabase — see `../supabase/README.md`. The standalone, no-account version of the dialer is in git history at commit `3540c465`.
